# Exact n=3000 to n=2000 fail-closed SFT handoff

Timestamp: `2026-07-17T01:58:19+08:00`

## Authorization and scope

The user requested automatic continuation from the currently running formal n=3000 SFT to n=2000
SFT. A new one-way watcher was implemented instead of resuming the stopped historical scale
supervisor. It is hard-bound to this exact parent:

`/home/wjy/pathvlm_r1_v1_formal/runs/stage1_sft/n3000_seed0042/formal_sft_n3000_seed0042_20260717_014544/run_manifest.yaml`

Its only possible child is one fresh n=2000, seed-42, ten-epoch formal SFT launched from the frozen
base model through `scripts/launch_formal_sft_run.sh`. The child does not inherit n=3000 weights.
The watcher cannot launch validation, test, another sample count, another seed, RL, Stage 2, or Stage
3. It never kills a GPU process.

## Transition gates

Before declaring the parent successful, the watcher requires:

- exact parent path and run ID, `status: completed`, and `formal_result: true`;
- exact PathMMU image-disjoint data version, n=3000 count, base model, revision, seed, topology,
  optimizer backend, freeze policy, and source-config hash;
- every recorded formal gate true, including final reload, nonzero finite gradients, language
  change, exact visual equality, test isolation, and maintained disk reserve;
- all ten expected epoch snapshots at steps 375 through 3750 with snapshot manifests;
- final checkpoint metadata present and the two-tier retention policy unchanged.

Immediately before the child launch, it re-verifies a clean and unchanged Git commit, the core code
manifest, its dedicated handoff code manifest, exact n=2000 generated-config SHA-256
`e495c64057a810e1c21bb44279e8b81f8159115eb5554aba7abc9d62cbccc8bd`, at least
1,157,493,686,272 free bytes, the exact eight-GPU inventory, and zero compute processes. If another
process occupies any GPU, it records the PIDs and waits without interference. The formal child
runner repeats its own atomic gates, including the master port.

The watcher fsyncs a launch intent before process creation, requires exactly one new n=2000
manifest, and never retries an unresolved launch automatically. The child starts in an independent
session. Any failed, failed-gate, aborted, cancelled, ambiguous, missing, or unknown state stops the
handoff.

## Persistent observability

Machine-local state is kept under:

`/home/wjy/pathvlm_r1_v1_formal/control/n3000_to_n2000_seed0042_20260717`

It contains an atomically replaced state JSON, append-only event JSONL, daemon log, child launcher
log, and exclusive lock. The watcher continues monitoring the n=2000 manifest after launch and marks
itself completed only if the child also passes the complete formal SFT gate set.

## Verification

- Both Python files compile.
- The Bash wrapper passes syntax validation and prints the exact approval scope.
- Sixteen isolated regression tests pass with warnings treated as errors.
- Tests cover valid parent/target manifests, false gates, failed parent, test access, wrong parent ID,
  missing snapshots, wrong config hash, path escape, disk threshold, busy GPU state, unique/multiple
  manifest discovery, unresolved launch intent, exclusive locking, and atomic state writes.
- No regression test launches training or touches the real run directory.

The watcher must not start until this document, its code/tests/wrapper, and
`protocol/n3000_to_n2000_handoff_manifest_20260717_015614.json` are committed and the repository is
clean. Starting the watcher is an engineering orchestration action, not a formal scientific result.
