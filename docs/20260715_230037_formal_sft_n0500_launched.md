# Formal SFT n=500 seed-42 launch

Timestamp: `2026-07-15 23:00:37 Asia/Shanghai`

## Launch decision

The collaborator-authorized Qwen3-VL vLLM service on physical GPUs 4--7 was stopped by the human
operator because it belonged to another Linux user and Codex correctly lacked permission to stop it.
A fresh check immediately before launch found all eight allocated RTX 4090 GPUs at the 18 MiB
baseline with no compute processes. No unrelated process was terminated by Codex.

The passing preflight, code-manifest, SFT environment, port, RAM, swap, and disk checks were rerun.
The first formal seed-42 SFT scale candidate was then launched through the audited fixed entry point:

```bash
source /home/wjy/pathvlm_r1_v1_formal/FORMAL_PATHS.env
bash scripts/launch_formal_sft_run.sh \
  --config /home/wjy/pathvlm_r1_v1_formal/generated_configs/sft/sft_n0500_seed0042.yaml
```

Run directory:

`/home/wjy/pathvlm_r1_v1_formal/runs/stage1_sft/n0500_seed0042/formal_sft_n0500_seed0042_20260715_230037`

## Frozen run definition

- Exact base revision: `cc594898137f460bfe9f0759e9844b3ce807cfb5`.
- Data version and adapter: `pathmmu_image_disjoint_v1`, `pathvlm_sft_n0500`, 500 QA.
- Seed: 42.
- Physical GPUs: 0--7; world size 8.
- Per-device batch 1, gradient accumulation 1, effective global batch 8.
- Ten epochs, 630 optimizer steps, learning rate 2e-5 with cosine schedule and 3% warmup.
- Full language-model fine-tuning; vision tower and multimodal projector frozen.
- ZeRO-3 with CPU optimizer offload; checkpoints every 100 steps, retain the last two plus final.
- Offline model/data access; validation and test are not accessed during training.

## Early health evidence

The model initialized with 7,615,616,512 trainable language parameters out of 8,292,166,656 total,
and logged both frozen visual components. The first optimizer step completed with loss `2.0167` and
gradient norm `35.69490051269531`. By step 37 all recorded losses and gradient norms remained finite
and gradients remained nonzero. Steady-state throughput was approximately 30.2 seconds per step.

During the initial resource sample, all eight GPUs were at 100% utilization and used approximately
8.9--9.7 GiB each. Host memory had approximately 82 GiB available, swap use was about 7.1 GiB, and
disk free space remained 2.0 TiB.

## Status and remaining gates

The run manifest remains `status: running` and `formal_result: false`. Completion still requires the
entire 630-step training run, retained/final checkpoint creation, independent final reload, finite
loss and nonzero-gradient history, changed language tensor, exactly unchanged visual tensor, and the
final manifest gates. No validation or later scale run may start concurrently.
