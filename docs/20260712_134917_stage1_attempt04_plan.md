# Stage 1 SFT Attempt 04 plan

Timestamp: 2026-07-12 13:49:17 Asia/Shanghai

## Reason for this attempt

GPU 0 is actively used by another user's training process and GPU 3 is occupied by another user's VLLM service. Only GPUs 1 and 2 have enough free memory. Attempt 03 remains prepared for a future three-free-GPU window and is not modified.

## Configuration

- GPUs: physical indices 1 and 2 only.
- Model: pinned Qwen2.5-VL-7B-Instruct snapshot.
- Method: full-parameter language-model SFT; vision tower and multimodal projector frozen.
- DeepSpeed: ZeRO-3 with parameter and optimizer CPU offload.
- Optimizer: Torch AdamW because DeepSpeedCPUAdam cannot compile against the current Torch/system CUDA mismatch.
- Attention: SDPA.
- Data: 8-sample smoke adapter; test is not accessed.
- Work: one optimizer step, per-device batch 1, no gradient accumulation.

## Success criteria

1. No interference with processes on GPUs 0 or 3.
2. Model/data initialization completes.
3. Forward, backward, and one optimizer update complete without dtype or OOM failure.
4. Final model save completes or, if saving is the only remaining failure, the optimizer-step success is independently evidenced in the log.
5. Peak GPU/RAM/swap/disk use and elapsed time are recorded.

## Stop criteria

- Stop only this run's exact processes if the host becomes unstable, swap approaches exhaustion, disk headroom becomes unsafe, or an unrelated workload is affected.
- Never terminate or modify unrelated users' processes.

## Expected limitation

CPU parameter offload is known to be slow on this host. This attempt tests functional correctness, not throughput. Formal full runs remain assigned to the separate compute machine.

